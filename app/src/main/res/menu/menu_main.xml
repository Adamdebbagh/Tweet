<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="tekwin.org.tweet.MainActivity">

    <item
        android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:icon="@mipmap/ic_action_settings"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_tweet"
        android:icon="@mipmap/ic_action_send_now"
        app:showAsAction="ifRoom"
        android:title="@string/tweet"/>
    <item
        android:id="@+id/action_refresh"
        android:icon="@mipmap/ic_action_refresh"
        app:showAsAction="ifRoom"
        android:title="@string/refresh"/>

    <item
        android:id="@+id/action_purge"
        android:icon="@mipmap/ic_action_remove"
        android:title="@string/purge">
    </item>
</menu>
